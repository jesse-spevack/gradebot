<div class="min-h-screen bg-gradient-to-b from-blue-50/30 via-white to-blue-50/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-blue-950 mb-4">Welcome, <%= Current.session.user.name %></h1>
      <p class="text-lg mb-8">
        We'll let you know when <span class="font-display font-black tracking-tight text-blue-950">GradeB<span class="inline-block align-baseline w-[1.4em] h-[1.4em] translate-y-[0.55em] -mx-[0.3em]" style="transform-origin: center;"><%= render "shared/robot" %></span>t</span> is ready
      </p>
      
      <%= link_to session_path, data: { turbo_method: :delete }, class: "inline-flex items-center justify-center px-6 py-2 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700" do %>
        Sign out
      <% end %>
    </div>
  </div>
</div>
