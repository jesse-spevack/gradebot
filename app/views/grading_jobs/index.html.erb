<div class="min-h-screen bg-gradient-to-b from-blue-50/30 via-white to-blue-50/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-blue-950 mb-4">Welcome, <%= Current.session.user.name %></h1>
      
      <div class="flex flex-col items-center justify-center gap-8 mb-12">
        <div class="max-w-2xl text-center">
          <p class="text-lg text-gray-600 mb-6">
            Select your Google Drive folder containing student assignments to begin grading.
            Make sure the folder contains all student submissions you want to evaluate.
          </p>
        </div>
        <%= render 'shared/folder_picker' %>
      </div>
      

    </div>
  </div>
</div>
