<%#
  Renders a step card with an icon, step number, title, and description
  
  Required parameters:
  - step_number: The step number (1, 2, 3, etc.)
  - title: The title of the step
  - description: The description of the step
  
  Optional parameters:
  - icon_path: SVG path data for the icon (default: appropriate icon based on step number)
  - container_classes: Additional classes for the container (default: "flex flex-col items-center text-center p-6")
%>

<%
  container_classes = local_assigns[:container_classes] || "flex flex-col items-center text-center p-6"
  
  # Default icons based on step number
  icon_path = local_assigns[:icon_path]
  if icon_path.nil?
    case step_number
    when 1
      icon_path = "M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"
    when 2
      icon_path = "M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"
    when 3
      icon_path = "M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
    else
      icon_path = "M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
    end
  end
%>

<div class="<%= container_classes %>">
  <div class="bg-indigo-100 rounded-full p-4 mb-4">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="<%= icon_path %>" />
    </svg>
  </div>
  <h3 class="text-xl font-semibold mb-2"><%= step_number %>. <%= title %></h3>
  <p class="text-gray-600"><%= description %></p>
</div>
