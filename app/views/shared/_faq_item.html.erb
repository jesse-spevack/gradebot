<%#
  Renders a FAQ item with question and answer
  
  Required parameters:
  - question: The question text
  - answer: The answer text
  
  Optional parameters:
  - container_classes: Additional classes for the container (default: "")
%>

<% container_classes = local_assigns[:container_classes] || "" %>

<div class="<%= container_classes %>">
  <h3 class="text-lg font-semibold mb-2"><%= question %></h3>
  <p class="text-gray-600"><%= answer %></p>
</div> 