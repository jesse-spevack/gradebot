<%# 
  Reusable social proof section that combines stats and trust badges
  
  Optional parameters:
  - first_stat: First statistic value
  - first_label: First statistic label
  - second_stat: Second statistic value
  - second_label: Second statistic label
  - badges: Array of badge text
%>

<div class="flex flex-col sm:flex-row items-center justify-center gap-8">
  <!-- Stats -->
  <%= render "shared/hero_stats", 
      first_stat: local_assigns[:first_stat], 
      first_label: local_assigns[:first_label],
      second_stat: local_assigns[:second_stat],
      second_label: local_assigns[:second_label] %>
  
  <!-- Trust badges -->
  <%= render "shared/trust_badges", badges: local_assigns[:badges] %>
</div> 